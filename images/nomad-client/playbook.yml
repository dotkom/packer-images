---
  - name: Setup image
    hosts: localhost
    connection: local
    become: yes
    vars:
      vault_role: nomad-client
    roles:
      - vault-agent
      - consul
      - consul-template
      - docker
      - nomad

