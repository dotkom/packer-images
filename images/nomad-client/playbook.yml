---
  - name: Setup image
    hosts: localhost
    connection: local
    become: yes
    vars:
      consul_template_vault_role: nomad-client
    roles:
      - consul
      - consul_template
      - docker
      - nomad

