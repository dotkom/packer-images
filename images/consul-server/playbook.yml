---
  - name: Setup image
    hosts: localhost
    connection: local
    become: yes
    vars: 
      consul_server: true
      consul_template_vault_role: consul-server
      consul_node_name_prefix: consul-server
    roles:
      - consul
      - consul-template